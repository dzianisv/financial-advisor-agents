__pycache__/
*.pyc
.telegraph_token
.telegraph_path
.tasks/

.DS_Store

# cached intraday OHLCV (re-fetch via crypto_data.py)
backtests/daytrade/data/

# connector audit trail (append-only, runtime)
connectors/audit_log.jsonl

# portfolio-monitor: never commit real holdings / reports (public repo)
stocks/portfolio-review.csv
stocks/monitor-report-*.md
.agents/skills/portfolio-monitor/scripts/__pycache__/

# local-only copyrighted source books; keep the catalog README tracked
books/*
!books/README.md

# local generated skill source lock metadata
skills-lock.json
forecasts/
13f/
.reflection/

# local copies created by npx skills add --copy (repo-root only — must NOT match .agents/skills/)
/skills/

# claude code session artifacts
session.txt
.claude/

# committee report outputs (recommendations — not committed)
reports/
crypto/news/
.agents/skills/crypto-news-store/.fetch_tmp.json
